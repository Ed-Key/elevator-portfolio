<!-- Keep PR bodies in the house voice: short prose and bullets, no heading
     stacks. Say what changed and why in a paragraph, list the pieces, show
     the pixels, state what was verified. -->

<!-- One or two sentences: the decision this PR makes, in domain language. -->

- <!-- the pieces of the change, one bullet each, with sizes for media -->

## Media

<!-- Visual changes always ship evidence: before/after screenshots or a
     short clip. Commit compact JPEGs under docs/superpowers/media/<branch-topic>/
     and embed them with commit-SHA raw URLs so links survive branch deletion:
     https://raw.githubusercontent.com/Ed-Key/elevator-portfolio/<sha>/docs/... -->

## Verified

<!-- npm run lint && npm run build, plus the browser pass for anything
     visual. Name what was actually checked, not "tested". End with where
     to look: the Vercel preview before releasing dev to main. -->
